[package]
name = "fsc_diagnostics"
version = "0.1.0"
edition = "2024"

[dependencies]
annotate-snippets = "0.12.16"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
